import { NotFoundContent } from '@/components/NotFoundContent';

/** notFound() anywhere in the English site. Unmatched URLs use global-not-found.tsx. */
export default function NotFound() {
    return <NotFoundContent />;
}
